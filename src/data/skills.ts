const skills: string[] = [
    'Building web apps using ReactJS (Intermediate level) / NextJS (Entry level)',
    'Creating restful API using Laravel (Entry Level)',
    'Unit testing (Entry Level)',
];

export default skills;
