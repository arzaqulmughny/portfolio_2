import tCategories from './tCategories';

type tProject = {
    categories: tCategories[];
    title: string;
    summary: string;
    url: string;
    thumbnail: string;
};

export default tProject;
